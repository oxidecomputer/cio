name: GitHub Actions Demo
on: [push]
jobs:
  test-secret-set-from-api:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{secrets.TEST_SECRET}}"
      - run: if [[ "${{secrets.TEST_SECRET}}" == "thing" ]]; then echo "success"; done
